# Fair Sber Price 💰

> **⚠️ Упрощенная версия** - Этот проект был упрощен для минимального использования ресурсов VPS. Полная версия с историей данных доступна в ветке `feature/full-version-with-history`.

Telegram-бот для расчета справедливой стоимости акций Сбербанка на основе P/B коэффициента. Веб-часть переведена в архивный (неактивный) режим.

## 🚀 Возможности

- **📊 Актуальная оценка** - получение текущих данных с Московской биржи и ЦБ РФ
- **📈 Инвестиционный анализ** - расчет P/B коэффициента и оценка справедливости цены
- **🤖 Telegram бот** - получение данных через бота с командами `/start`, `/info`, `/thesis`, `/method`, `/range`, `/risks`, `/help`
- **🔄 Кеширование** - оптимизированные запросы с in-memory кешированием
- **⚡ Легковесность** - минимальное использование ресурсов (~150-250 MB RAM)

## 🏗️ Архитектура (упрощенная)

### Основные компоненты:
- **Telegram бот** - основной интерфейс
- **Service Layer** - бизнес-логика и работа с внешними API
- **Django core** - инфраструктура проекта (settings, ORM, management commands)
- **In-memory кеширование** - оптимизация без Redis
- **SQLite** - служебная БД Django

### Источники данных:
- **MOEX API** - текущие цены акций Сбербанка
- **ЦБ РФ** - данные о собственном капитале банка
- **MOEX failover** - переключение между HTTPS/HTTP endpoint (настраивается через `MOEX_BASE_URLS`)
- **Тюнинг таймаутов API** - параметры `MOEX_REQUEST_TIMEOUT`, `MOEX_MAX_TOTAL_SECONDS`, `CBR_REQUEST_TIMEOUT` для контроля скорости ответа бота

### Что убрано в упрощенной версии:
- ❌ PostgreSQL (используется SQLite только для Django admin)
- ❌ Redis (используется in-memory кеш)
- ❌ История данных и снапшоты
- ❌ Команда `/history` в боте
- ❌ Логирование в файлы (только console)
- ❌ Cron задачи для сохранения данных

## 🛠️ Локальная разработка

### Быстрый старт
```bash
# 1. Клонируйте репозиторий
git clone https://github.com/grigra27/fsp.git
cd fsp

# 2. Настройте окружение
cd fsp
python -m venv venv
source venv/bin/activate  # Linux/Mac
pip install -r requirements.txt

# 3. Настройте переменные окружения
cp ../.env.example ../.env
# Отредактируйте .env файл

# 4. Запустите проект
python manage.py migrate
python manage.py runtelegrambot
```

### Запуск Telegram бота (в отдельном терминале)
```bash
cd fsp
source venv/bin/activate
python manage.py runtelegrambot
```

## 📊 Использование

### Telegram бот
Команды:
- `/start` - приветствие, меню и текущие данные
- `/info` - актуальная оценка
- `/thesis` - инвестиционный тезис
- `/method` - почему P/B = 1
- `/range` - почему диапазон 1.0–1.2
- `/risks` - ключевые риски
- `/help` - справка по командам

## 🚀 Деплой в продакшн

### Автоматический деплой (рекомендуется)
1. **Настройте GitHub Secrets** - см. раздел "Деплой в продакшн" в [DOCUMENTATION.md](DOCUMENTATION.md)
2. **Push в main ветку** - GitHub Actions автоматически задеплоит приложение
3. **Готово!** Бот доступен пользователям в Telegram

### Ручной деплой
```bash
# На сервере
git clone https://github.com/grigra27/fsp.git fair-sber-price
cd fair-sber-price
cp .env.example .env  # Отредактируйте настройки
docker-compose -f docker-compose.prod.yml up -d
```

### Управление проектом
```bash
docker-compose -f docker-compose.prod.yml ps      # Статус
docker-compose -f docker-compose.prod.yml logs    # Логи
docker-compose -f docker-compose.prod.yml restart # Перезапуск
```

### Архитектура деплоя (упрощенная)
- **Bot Service** - Telegram бот
- **Django core** - сервисный слой и конфигурация
- **SQLite** - минимальная БД для Django admin

Архивный веб-стек сохранен в `docker-compose.web-archive.yml` и описан в `WEB_ARCHIVE.md`.

### Требования к VPS:
- **RAM**: 256 MB минимум, 512 MB рекомендуется
- **Диск**: ~100 MB
- **CPU**: 1 ядро достаточно

📖 **Документация**:
- [DOCUMENTATION.md](DOCUMENTATION.md) - полная документация по проекту

## 📈 Логика оценки

### P/B коэффициент (Price-to-Book)
- **< 1.0** - 🟢 дешево
- **1.0 - 1.2** - 🔵 справедливо  
- **1.2 - 1.4** - 🟡 чуть дорого
- **> 1.4** - 🔴 дорого

### Расчет справедливой цены
```
Справедливая цена = Собственный капитал / Количество акций
P/B = Рыночная цена / Справедливая цена
```

## 🔄 Полная версия с историей

Если нужна полная версия с PostgreSQL, Redis и историей данных:
```bash
git checkout feature/full-version-with-history
```

Полная версия включает:
- ✅ PostgreSQL для хранения истории
- ✅ Redis для продвинутого кеширования
- ✅ Команда `/history` в боте
- ✅ Сохранение снапшотов данных
- ✅ Логирование в файлы
- ✅ API для исторических данных

## 🔒 Безопасность

- Все секретные ключи вынесены в переменные окружения
- CSRF защита включена
- Валидация входных данных
- Автоматические security headers в продакшене

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

## ⚠️ Дисклеймер

Данная информация носит исключительно информационный характер и не является инвестиционной рекомендацией или советом по покупке/продаже ценных бумаг. Всегда проводите собственный анализ и консультируйтесь с финансовыми консультантами перед принятием инвестиционных решений.

## 📞 Поддержка

Если у вас есть вопросы или предложения:
- Создайте Issue в GitHub

---

**Fair Sber Price** - инструмент для осознанного инвестирования 📊💡